{
  "type": "object",
  "properties": {
    "channelId": {
      "type": "number",
      "description": "The id of the channel to watch for followers"
    },
    "updateFollowersInterval": {
      "type": "number",
      "default": 60,
      "description": "Interval to update followers (seconds)"
    }
  },
  "required": ["channelId"]
}
